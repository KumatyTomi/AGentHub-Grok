{
  "name": "@agenthub/mesh-core",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local-only AgentMesh cluster core for LAN (Windows/Linux). No cloud.",
  "bin": {
    "mesh-core": "./server.mjs"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs",
    "doctor": "node scripts/doctor.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
